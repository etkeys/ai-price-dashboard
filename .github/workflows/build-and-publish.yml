name: Build and publish

on:
  pull_request:
    types: [opened, synchronize, reopened]
  release:
    types: [published]

permissions:
  contents: read

env:
  REGISTRY: cr.etkeys.xyz:31500
  IMAGE_NAME: ai-price-dashboard

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install dependencies
        run: pip install -e ".[dev]"

      - name: Run tests
        run: pytest

  build-and-publish:
    needs: test
    runs-on: ubuntu-latest
    if: github.event_name == 'release' || github.event.pull_request.head.repo.full_name == github.repository
    steps:
      - name: Connect to Tailscale
        uses: tailscale/github-action@v4
        with:
          oauth-client-id: ${{ secrets.TAILSCALE_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TAILSCALE_OAUTH_SECRET }}
          tags: tag:ci
          ping: cr.etkeys.xyz

      - name: Trust private registry certificate
        run: |
          printf '%s\n' "${{ secrets.REGISTRY_CERT }}" \
            | sudo tee /usr/local/share/ca-certificates/cr.etkeys.xyz.crt > /dev/null
          sudo update-ca-certificates
          sudo systemctl restart docker

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Compute image tags
        id: meta
        run: |
          IMAGE="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}"
          if [ "${{ github.event_name }}" = "release" ]; then
            VERSION="${{ github.event.release.tag_name }}"
            echo "tag_version=${IMAGE}:${VERSION}" >> "$GITHUB_OUTPUT"
            echo "tag_latest=${IMAGE}:latest" >> "$GITHUB_OUTPUT"
          else
            echo "tag_version=${IMAGE}:pr-${{ github.event.pull_request.number }}" >> "$GITHUB_OUTPUT"
          fi

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build docker image
        run: |
          if [ "${{ github.event_name }}" = "release" ]; then
            docker build -t "${{ steps.meta.outputs.tag_version }}" -t "${{ steps.meta.outputs.tag_latest }}" .
          else
            docker build -t "${{ steps.meta.outputs.tag_version }}" .
          fi

      - name: Log in to private registry
        run: |
          printf '%s' "${{ secrets.REGISTRY_PASSWORD }}" \
            | docker login ${{ env.REGISTRY }} \
                -u "${{ secrets.REGISTRY_USER }}" --password-stdin

      - name: Push docker image
        run: |
          docker push "${{ steps.meta.outputs.tag_version }}"
          if [ "${{ github.event_name }}" = "release" ]; then
            docker push "${{ steps.meta.outputs.tag_latest }}"
          fi

      - name: Log out of private registry
        if: always()
        run: docker logout ${{ env.REGISTRY }}
